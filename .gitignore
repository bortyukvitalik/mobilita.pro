# Секреты
.env

# Серверные файлы (создаются на VPS, в репо не хранятся):
# nginx/.htpasswd — хэш basic-auth стейджа; override.yml монтирует его
nginx/.htpasswd
docker-compose.override.yml

# macOS AppleDouble (артефакты rsync)
._*

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store

# Node (только для сборки Tailwind)
node_modules/
package-lock.json

# Локальные инструменты (preview-конфиг с абсолютными путями этой машины)
.claude/
